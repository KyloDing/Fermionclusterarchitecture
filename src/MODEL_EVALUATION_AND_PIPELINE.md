# 模型评测与 Pipeline 编排模块

## 📋 模块概览

**创建日期**：2024-11-14  
**版本**：v1.0  
**状态**：✅ 核心功能已完成

本文档介绍费米集群系统新增的两大核心模块：
1. **模型评测模块** - 对大语言模型进行全面的能力评估和性能测试
2. **Pipeline 编排模块** - 基于 Kubeflow 实现从数据预处理到模型部署的流程化编排

---

## 🎯 模型评测模块

### 功能概述

模型评测模块提供了一套完整的大模型评测解决方案，支持多种评测类型和丰富的评测指标。

### 核心功能

#### 1. 评测任务管理

**支持的评测类型**：
- **基准评测（Benchmark）** - 使用标准数据集进行能力测试
- **自定义评测（Custom）** - 使用自定义数据集和指标
- **对比评测（Comparison）** - 多模型横向对比

**任务状态**：
```typescript
type EvaluationStatus = 
  | 'pending'    // 等待中
  | 'running'    // 运行中  
  | 'completed'  // 已完成
  | 'failed'     // 失败
  | 'cancelled'; // 已取消
```

#### 2. 评测指标体系

**四大类别**：

1. **质量指标（Quality）**
   - 准确率（Accuracy）
   - BLEU Score - 机器翻译质量
   - ROUGE Score - 文本摘要质量
   - 困惑度（Perplexity）

2. **性能指标（Performance）**
   - 推理延迟（Latency）- 单次推理平均延迟
   - 吞吐量（Throughput）- 每秒处理请求数

3. **安全指标（Safety）**
   - 毒性检测（Toxicity）- 生成内容的毒性评分
   - 偏见检测（Bias）- 模型输出的偏见程度

4. **效率指标（Efficiency）**
   - 资源利用率
   - 成本效益比

#### 3. 评测数据集

**内置基准数据集**：

| 数据集 | 类型 | 语言 | 样本数 | 说明 |
|--------|------|------|--------|------|
| **MMLU** | QA | 英文 | 15,908 | 大规模多任务语言理解 |
| **C-Eval** | QA | 中文 | 13,948 | 中文综合能力评估（52个学科） |
| **HumanEval** | Coding | Python | 164 | Python代码生成能力 |
| **GSM8K** | Reasoning | 英文 | 8,500 | 小学数学应用题 |
| **TruthfulQA** | QA | 英文 | 817 | 真实准确答案评估 |

**支持类型**：
- QA（问答）
- Generation（生成）
- Classification（分类）
- Reasoning（推理）
- Coding（编程）
- Multilingual（多语言）

#### 4. 评测配置

**模型参数**：
```typescript
{
  batchSize: number;        // 批处理大小
  temperature?: number;     // 温度参数
  maxTokens?: number;       // 最大token数
  topP?: number;           // Top-P采样
  topK?: number;           // Top-K采样
  repeatPenalty?: number;  // 重复惩罚
}
```

**计算资源**：
```typescript
{
  gpuType: string;      // GPU型号（如 NVIDIA A100）
  gpuCount: number;     // GPU数量
  memoryGB: number;     // 内存大小
  storageGB: number;    // 存储大小
}
```

### 数据结构

#### 评测任务

```typescript
interface EvaluationTask {
  id: string;
  name: string;
  description: string;
  type: 'benchmark' | 'custom' | 'comparison';
  status: EvaluationStatus;
  
  // 模型信息
  modelId: string;
  modelName: string;
  modelVersion: string;
  modelType: 'llm' | 'vlm' | 'embedding' | 'classification';
  
  // 对比模型（用于对比评测）
  comparisonModels?: {
    modelId: string;
    modelName: string;
    modelVersion: string;
  }[];
  
  // 评测配置
  datasets: string[];      // 数据集ID列表
  metrics: string[];       // 指标ID列表
  config: EvaluationConfig;
  computeConfig: ComputeConfig;
  
  // 执行信息
  startTime?: string;
  endTime?: string;
  duration?: number;       // 秒
  progress: number;        // 0-100
  
  // 结果
  results?: EvaluationResult[];
  overallScore?: number;   // 综合评分
  reportUrl?: string;      // 报告下载链接
  
  // 元数据
  createdBy: string;
  createdAt: string;
  updatedAt: string;
  tags: string[];
}
```

#### 评测结果

```typescript
interface EvaluationResult {
  metricId: string;
  metricName: string;
  value: number;
  unit?: string;
  rank?: number;         // 排名（对比评测）
  percentile?: number;   // 百分位
}
```

### UI 功能

#### 主页面

**路径**：`/model-evaluation`

**功能模块**：

1. **统计概览**
   - 总任务数
   - 运行中任务
   - 已完成任务
   - 等待中任务
   - 失败任务

2. **Tab 导航**
   - 评测任务列表
   - 评测模板库
   - 排行榜
   - 评测数据集管理

3. **任务列表**
   - 搜索和筛选
   - 状态筛选
   - 类型筛选
   - 进度显示
   - 综合评分显示

4. **操作功能**
   - 新建评测任务
   - 查看详情
   - 取消运行中的任务
   - 下载评测报告
   - 删除任务

#### 任务创建流程

```
1. 选择评测类型
   ├─ 基准评测：使用标准数据集
   ├─ 自定义评测：自定义数据集和指标
   └─ 对比评测：多模型横向对比

2. 选择模型
   ├─ 从模型仓库选择
   └─ 指定模型版本

3. 配置评测
   ├─ 选择数据集（支持多选）
   ├─ 选择评测指标
   └─ 配置模型参数

4. 配置资源
   ├─ GPU类型和数量
   ├─ 内存大小
   └─ 存储大小

5. 提交运行
   └─ 监控进度和查看结果
```

### API 接口

```typescript
// 获取评测任务列表
getEvaluationTasks(): Promise<EvaluationTask[]>

// 获取单个评测任务
getEvaluationTask(id: string): Promise<EvaluationTask | null>

// 创建评测任务
createEvaluationTask(data: Partial<EvaluationTask>): Promise<EvaluationTask>

// 取消评测任务
cancelEvaluationTask(id: string): Promise<void>

// 删除评测任务
deleteEvaluationTask(id: string): Promise<void>

// 获取评测指标列表
getEvaluationMetrics(): Promise<EvaluationMetric[]>

// 获取评测数据集列表
getEvaluationDatasets(): Promise<EvaluationDataset[]>

// 获取评测模板
getEvaluationTemplates(): Promise<EvaluationTemplate[]>
```

---

## 🔄 Pipeline 编排模块

### 功能概述

基于 Kubeflow Pipelines 的流程编排系统，支持从数据预处理到模型部署的完整 MLOps 流程。

### 核心概念

#### Pipeline（流水线）

一个完整的机器学习工作流，由多个步骤（Steps）组成，步骤之间通过依赖关系连接。

#### Step（步骤）

流水线中的单个执行单元，可以是：
- 数据预处理
- 数据增强
- 模型训练
- 模型评测
- 模型优化
- 模型部署
- 自定义步骤

### 支持的步骤类型

```typescript
type StepType = 
  | 'data-preparation'    // 📊 数据预处理
  | 'data-augmentation'   // 🔄 数据增强
  | 'model-training'      // 🎯 模型训练
  | 'model-evaluation'    // 📈 模型评测
  | 'model-optimization'  // ⚡ 模型优化
  | 'model-deployment'    // 🚀 模型部署
  | 'custom';             // ⚙️ 自定义步骤
```

### Pipeline 生命周期

```
1. 草稿（Draft）
   └─ 正在编辑，未运行

2. 运行中（Running）
   ├─ 步骤按依赖关系顺序执行
   └─ 实时监控进度

3. 已完成（Completed）
   ├─ 所有步骤成功完成
   └─ 生成输出和报告

4. 失败（Failed）
   ├─ 某个步骤执行失败
   └─ 可查看错误日志

5. 已暂停（Paused）
   └─ 手动暂停执行
```

### 数据结构

#### Pipeline 定义

```typescript
interface Pipeline {
  id: string;
  name: string;
  description: string;
  version: string;
  status: 'draft' | 'running' | 'completed' | 'failed' | 'paused';
  
  // 步骤列表
  steps: PipelineStep[];
  
  // 全局配置
  config: {
    timeout?: number;          // 超时时间（分钟）
    retryPolicy?: {
      maxRetries: number;      // 最大重试次数
      backoff: 'linear' | 'exponential';
    };
    notifications?: {
      email?: string[];        // 邮件通知
      webhook?: string;        // Webhook URL
    };
  };
  
  // 调度配置
  schedule?: {
    enabled: boolean;
    cron?: string;             // Cron 表达式
    timezone?: string;
  };
  
  // 元数据
  category: 'training' | 'inference' | 'data-processing' | 'end-to-end';
  tags: string[];
  isTemplate: boolean;
  
  // 统计信息
  runCount: number;
  successCount: number;
  failureCount: number;
  
  // 时间信息
  createdBy: string;
  createdAt: string;
  updatedAt: string;
  lastRunAt?: string;
}
```

#### Pipeline 步骤

```typescript
interface PipelineStep {
  id: string;
  name: string;
  type: StepType;
  description?: string;
  
  // 可视化位置
  position: { x: number; y: number };
  
  // 依赖关系
  dependencies: string[];  // 依赖的步骤ID列表
  
  // 容器配置
  config: {
    image?: string;         // 容器镜像
    command?: string[];     // 执行命令
    args?: string[];        // 命令参数
    env?: Record<string, string>;
    resources?: {
      cpuCores: number;
      memoryGB: number;
      gpuCount?: number;
      gpuType?: string;
    };
  };
  
  // 输入输出
  inputs?: {
    name: string;
    type: 'dataset' | 'model' | 'config' | 'artifact';
    source?: string;       // 来源步骤ID或外部路径
  }[];
  outputs?: {
    name: string;
    type: 'dataset' | 'model' | 'metrics' | 'artifact';
    path: string;
  }[];
  
  // 执行状态
  status?: 'pending' | 'running' | 'completed' | 'failed' | 'skipped';
  startTime?: string;
  endTime?: string;
  duration?: number;
  logs?: string;
  error?: string;
}
```

#### Pipeline 运行记录

```typescript
interface PipelineRun {
  id: string;
  pipelineId: string;
  pipelineName: string;
  version: string;
  status: PipelineStatus;
  
  // 触发信息
  trigger: 'manual' | 'scheduled' | 'api';
  triggeredBy: string;
  
  // 步骤执行状态
  stepStatuses: Record<string, StepStatus>;
  currentStep?: string;
  
  // 时间信息
  startTime: string;
  endTime?: string;
  duration?: number;
  
  // 资源使用
  resourceUsage?: {
    cpuHours: number;
    memoryGBHours: number;
    gpuHours?: number;
  };
  
  // 成本
  cost?: number;
  
  // 输出
  outputs?: {
    models?: string[];
    datasets?: string[];
    metrics?: Record<string, any>;
    artifacts?: string[];
  };
}
```

### UI 功能

#### 主页面

**路径**：`/pipeline-orchestration`

**功能模块**：

1. **统计概览**
   - 总流水线数
   - 运行中流水线
   - 已完成流水线
   - 草稿流水线
   - 失败流水线

2. **Tab 导航**
   - 流水线列表
   - 运行历史
   - 模板库

3. **流水线列表**
   - 搜索和筛选
   - 状态筛选
   - 类别筛选
   - 步骤数量显示
   - 成功率统计

4. **运行历史**
   - 运行记录列表
   - 触发方式
   - 资源使用统计
   - 成本统计

5. **模板库**
   - 内置模板
   - 使用量统计
   - 评分显示
   - 一键创建

#### Pipeline 编辑器（规划中）

**可视化编排功能**：
```
┌─────────────────────────────────────┐
│  [工具栏]                           │
├─────────────────────────────────────┤
│  [步骤面板]  │  [画布区域]         │
│              │                      │
│  📊 数据预处理│   ┌──┐             │
│  🔄 数据增强  │   │S1│─────┐       │
│  🎯 模型训练  │   └──┘     │       │
│  📈 模型评测  │          ┌─▼─┐     │
│  ⚡ 模型优化  │          │S2 │     │
│  🚀 模型部署  │          └─┬─┘     │
│  ⚙️ 自定义    │            │       │
│              │          ┌─▼─┐     │
│              │          │S3 │     │
│              │          └───┘     │
└──────────────┴─────────────────────┘
```

**拖拽式操作**：
- 从步骤面板拖拽到画布
- 连线建立依赖关系
- 双击配置步骤参数
- 自动布局优化

### 内置模板

#### 1. 大语言模型微调

```yaml
name: LLM FineTuning Pipeline
category: training
steps:
  - name: 数据预处理
    type: data-preparation
    resources:
      cpu: 8核
      memory: 32GB
  
  - name: 模型训练
    type: model-training
    resources:
      cpu: 16核
      memory: 64GB
      gpu: 4x NVIDIA A100
  
  - name: 模型评测
    type: model-evaluation
    resources:
      cpu: 8核
      memory: 32GB
      gpu: 1x NVIDIA A100
  
  - name: 模型部署
    type: model-deployment
    resources:
      cpu: 4核
      memory: 16GB
```

#### 2. 计算机视觉模型训练

```yaml
name: CV Model Training Pipeline
category: training
steps:
  - name: 图像预处理
    type: data-preparation
  
  - name: 模型训练
    type: model-training
  
  - name: 模型评测
    type: model-evaluation
```

### API 接口

```typescript
// 获取流水线列表
getPipelines(): Promise<Pipeline[]>

// 获取单个流水线
getPipeline(id: string): Promise<Pipeline | null>

// 创建流水线
createPipeline(data: Partial<Pipeline>): Promise<Pipeline>

// 更新流水线
updatePipeline(id: string, data: Partial<Pipeline>): Promise<Pipeline | null>

// 删除流水线
deletePipeline(id: string): Promise<void>

// 运行流水线
runPipeline(id: string): Promise<PipelineRun>

// 获取运行历史
getPipelineRuns(pipelineId?: string): Promise<PipelineRun[]>

// 获取模板列表
getPipelineTemplates(): Promise<PipelineTemplate[]>

// 从模板创建流水线
createPipelineFromTemplate(
  templateId: string, 
  params: Record<string, any>
): Promise<Pipeline>
```

---

## 🎨 视觉设计

### 配色方案

**模型评测模块**：
- 主色：`purple-600` - 紫色代表智能和创新
- 辅色：`blue-500` - 蓝色代表稳定和专业
- 成功：`emerald-500` - 绿色代表成功完成
- 警告：`amber-500` - 黄色代表等待和警告
- 错误：`red-500` - 红色代表失败和错误

**Pipeline 编排模块**：
- 主色：`purple-600` - 保持品牌一致性
- 步骤类型颜色：
  - 数据处理：`blue-500`
  - 模型训练：`purple-500`
  - 模型评测：`emerald-500`
  - 模型部署：`sky-500`

### 图标系统

| 功能 | 图标 | 含义 |
|------|------|------|
| 模型评测 | `TrendingUp` | 性能评估、数据上升 |
| Pipeline | `GitBranch` | 流程分支、工作流 |
| 运行中 | `Loader2` | 加载动画 |
| 已完成 | `CheckCircle2` | 成功完成 |
| 失败 | `AlertCircle` | 错误警告 |
| 步骤 | `Layers` | 层级结构 |

---

## 📊 使用场景

### 场景 1：大模型综合评测

**需求**：
对新训练的 Qwen2-7B 模型进行全面评估

**步骤**：
1. 创建基准评测任务
2. 选择模型：Qwen2-7B-Instruct v1.0
3. 选择数据集：MMLU、C-Eval、HumanEval
4. 选择指标：准确率、延迟、吞吐量
5. 配置资源：1x A100、40GB内存
6. 提交运行
7. 查看评测报告和综合评分

**预期结果**：
- 多维度评估结果
- 性能指标对比
- 可下载的PDF报告

### 场景 2：模型对比评测

**需求**：
对比 GLM-4 和 Qwen2 在中文任务上的表现

**步骤**：
1. 创建对比评测任务
2. 主模型：GLM-4-9B
3. 对比模型：Qwen2-7B
4. 选择数据集：C-Eval、GSM8K
5. 选择指标：准确率、困惑度
6. 提交运行
7. 查看对比结果和排名

**预期结果**：
- 并排对比结果
- 排名和百分位
- 优劣势分析

### 场景 3：端到端训练流水线

**需求**：
从数据预处理到模型部署的完整流程

**步骤**：
1. 选择模板：大语言模型微调
2. 配置参数：
   - 基座模型：Qwen2-7B
   - 训练数据集：dataset-001
   - 学习率：0.0001
3. 自动创建 Pipeline：
   - 步骤1：数据预处理（8核CPU、32GB内存）
   - 步骤2：数据增强（4核CPU、16GB内存）
   - 步骤3：模型训练（16核CPU、64GB内存、4xA100）
   - 步骤4：模型评测（8核CPU、32GB内存、1xA100）
   - 步骤5：模型部署（4核CPU、16GB内存）
4. 运行 Pipeline
5. 监控进度和资源使用
6. 查看输出模型和评测结果

**预期结果**：
- 完整的训练记录
- 中间输出artifact
- 最终部署的模型
- 资源使用和成本统计

### 场景 4：定时训练任务

**需求**：
每周自动运行模型重训练流程

**步骤**：
1. 创建或编辑 Pipeline
2. 配置调度：
   ```
   启用：是
   Cron：0 2 * * 1  # 每周一凌晨2点
   时区：Asia/Shanghai
   ```
3. 配置通知：
   ```
   邮件：ml-team@company.com
   Webhook：https://api.company.com/notifications
   ```
4. 保存配置

**预期结果**：
- 自动定时执行
- 执行完成后发送通知
- 运行历史记录

---

## 🔧 技术实现

### 服务层

#### 模型评测服务

**文件**：`/services/evaluationService.ts`

**Mock 数据**：
- 8 个评测指标
- 5 个评测数据集
- 3 个示例任务
- 3 个评测模板

**核心函数**：
- 任务 CRUD
- 指标和数据集查询
- 模板管理

#### Pipeline 服务

**文件**：`/services/pipelineService.ts`

**Mock 数据**：
- 2 个示例 Pipeline
- 1 个运行记录
- 2 个内置模板

**核心函数**：
- Pipeline CRUD
- 运行管理
- 模板实例化

### 组件层

#### 页面组件

1. **ModelEvaluationPage.tsx**
   - 任务列表
   - 统计卡片
   - 筛选和搜索
   - 操作菜单

2. **PipelineOrchestrationPage.tsx**
   - Pipeline 列表
   - 运行历史
   - 模板库
   - 统计概览

### 路由配置

```typescript
// App.tsx
<Route path="/model-evaluation" element={<ModelEvaluationPage />} />
<Route path="/pipeline-orchestration" element={<PipelineOrchestrationPage />} />
```

### 导航配置

```typescript
// AppSidebar.tsx
{
  group: '数据资产',
  items: [
    { id: 'images', label: '镜像管理', icon: Package },
    { id: 'datasets', label: '数据集', icon: Database },
    { id: 'models', label: '模型仓库', icon: Box },
    { id: 'model-evaluation', label: '模型评测', icon: TrendingUp },
    { id: 'pipeline-orchestration', label: 'Pipeline编排', icon: GitBranch },
  ],
}
```

---

## 📈 后续开发计划

### Phase 1（当前阶段）✅
- [x] 模型评测任务列表
- [x] Pipeline 流水线列表
- [x] 基础统计和筛选
- [x] 运行历史记录
- [x] 模板库展示

### Phase 2（下一阶段）
- [ ] 创建评测任务对话框
- [ ] 评测任务详情页面
- [ ] 评测结果可视化
- [ ] 排行榜功能
- [ ] Pipeline 可视化编辑器
- [ ] Pipeline 运行详情页面

### Phase 3（功能增强）
- [ ] 实时日志查看
- [ ] 步骤级别的重试
- [ ] 分支和条件执行
- [ ] 参数化 Pipeline
- [ ] Webhook 集成
- [ ] Slack/钉钉通知

### Phase 4（高级功能）
- [ ] 自定义评测指标
- [ ] 评测数据集上传
- [ ] Pipeline 版本管理
- [ ] Pipeline 导入/导出
- [ ] 成本优化建议
- [ ] 自动调优

---

## 🎯 最佳实践

### 评测任务

1. **选择合适的数据集**
   - 根据模型类型选择相关数据集
   - 平衡数据集规模和评测时间
   - 使用多个数据集获得全面评估

2. **配置合理的资源**
   - 根据模型大小分配GPU
   - 预留足够的内存空间
   - 考虑评测时间限制

3. **解读评测结果**
   - 综合多个指标判断
   - 注意指标的适用场景
   - 与基线模型对比

### Pipeline 设计

1. **模块化设计**
   - 将复杂流程拆分为独立步骤
   - 每个步骤单一职责
   - 便于复用和维护

2. **依赖关系**
   - 明确步骤间的依赖
   - 避免循环依赖
   - 并行执行无依赖步骤

3. **资源优化**
   - 合理分配计算资源
   - 避免资源浪费
   - 考虑成本因素

4. **错误处理**
   - 配置重试策略
   - 设置合理的超时
   - 记录详细日志

5. **版本管理**
   - 为 Pipeline 设置版本号
   - 记录重要变更
   - 保持向后兼容

---

## 💡 常见问题

### Q1: 评测任务运行时间过长怎么办？

**A:** 可以通过以下方式优化：
- 减少数据集规模或采样
- 增加 GPU 数量
- 提高批处理大小
- 选择更快的评测指标

### Q2: Pipeline 执行失败如何排查？

**A:** 排查步骤：
1. 查看失败步骤的错误日志
2. 检查资源配置是否充足
3. 验证输入数据是否正确
4. 检查依赖关系是否合理
5. 尝试单独运行失败的步骤

### Q3: 如何对比不同版本模型的评测结果？

**A:** 方法：
1. 使用对比评测功能
2. 保持评测配置一致
3. 使用相同的数据集和指标
4. 查看并排对比结果

### Q4: Pipeline 能否在执行中修改？

**A:** 
- 运行中的 Pipeline 不能修改
- 可以暂停后修改再继续
- 建议创建新版本 Pipeline

### Q5: 评测结果如何分享？

**A:** 支持多种方式：
- 下载 PDF 报告
- 导出 JSON 数据
- 分享任务链接
- 提交到排行榜

---

## 📚 参考资料

### 评测基准

- [MMLU](https://arxiv.org/abs/2009.03300) - Massive Multitask Language Understanding
- [C-Eval](https://cevalbenchmark.com/) - 中文综合能力评估
- [HumanEval](https://github.com/openai/human-eval) - Python代码生成评估
- [GSM8K](https://github.com/openai/grade-school-math) - 数学推理评估

### Kubeflow Pipelines

- [Kubeflow Pipelines 文档](https://www.kubeflow.org/docs/components/pipelines/)
- [Pipeline SDK](https://kubeflow-pipelines.readthedocs.io/)
- [最佳实践](https://www.kubeflow.org/docs/components/pipelines/best-practices/)

### MLOps

- [MLOps 原则](https://ml-ops.org/)
- [Google Cloud MLOps](https://cloud.google.com/architecture/mlops-continuous-delivery-and-automation-pipelines-in-machine-learning)

---

## 🎉 总结

模型评测与 Pipeline 编排模块为费米集群系统提供了完整的 MLOps 能力：

✅ **模型评测** - 全面评估模型性能和质量  
✅ **流程编排** - 自动化从数据到部署的全流程  
✅ **可视化管理** - 直观的 UI 和丰富的统计信息  
✅ **灵活扩展** - 支持自定义指标和步骤  
✅ **企业级** - 权限控制、资源管理、成本统计

这两个模块是构建智能化AI平台的重要基础设施，为用户提供了专业的模型开发和部署工具链。

---

**文档版本**：v1.0  
**最后更新**：2024-11-14  
**维护者**：费米集群开发团队
