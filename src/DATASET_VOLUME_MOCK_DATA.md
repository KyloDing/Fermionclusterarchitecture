# 从存储卷创建数据集 - 模拟数据说明

## 📊 模拟数据结构

### 根目录 (`/`)
```
/
├── datasets/          # 数据集目录
├── workspace/         # 工作空间目录
├── experiments/       # 实验数据目录
├── models/           # 模型目录
├── temp/             # 临时文件目录
└── README.md         # 说明文件
```

### datasets 目录 (`/datasets/`)
包含多种类型的示例数据集：
```
/datasets/
├── imagenet-2024/              # 图像分类数据集
├── cifar10-augmented/          # 扩增的 CIFAR-10 数据集
├── coco-detection/             # 目标检测数据集
├── custom-dataset-v1/          # 自定义数据集
├── text-corpus/                # 文本语料库
├── audio-samples/              # 音频样本
├── video-clips/                # 视频片段
└── dataset-index.json          # 数据集索引文件
```

### imagenet-2024 详细结构 (`/datasets/imagenet-2024/`)
完整的图像数据集结构：
```
/datasets/imagenet-2024/
├── train/                      # 训练集
│   ├── n01440764/             # 类别目录 1
│   ├── n01443537/             # 类别目录 2
│   ├── n01484850/             # 类别目录 3
│   ├── n01491361/             # 类别目录 4
│   ├── n01494475/             # 类别目录 5
│   └── n01496331/             # 类别目录 6
├── val/                        # 验证集
├── test/                       # 测试集
├── metadata.json               # 元数据文件 (8KB)
├── labels.txt                  # 标签文件 (10KB)
└── statistics.csv              # 统计信息 (5KB)
```

### cifar10-augmented 结构 (`/datasets/cifar10-augmented/`)
按类别组织的数据集：
```
/datasets/cifar10-augmented/
├── airplane/                   # 飞机类别
├── automobile/                 # 汽车类别
├── bird/                       # 鸟类类别
├── cat/                        # 猫类类别
├── deer/                       # 鹿类类别
├── dog/                        # 狗类类别
├── frog/                       # 青蛙类别
├── horse/                      # 马类类别
├── ship/                       # 船类类别
├── truck/                      # 卡车类别
└── augmentation_config.yaml    # 扩增配置 (2KB)
```

### workspace 目录 (`/workspace/`)
用户工作空间，包含处理后的数据：
```
/workspace/
├── processed/                  # 处理后的数据
│   ├── dataset-v1/            # 版本 1
│   ├── dataset-v2/            # 版本 2
│   ├── dataset-v3/            # 版本 3
│   ├── filtered-data/         # 过滤后的数据
│   ├── merged-data/           # 合并后的数据
│   └── processing_log.txt     # 处理日志 (25KB)
├── augmented/                  # 扩增后的数据
│   ├── rotation/              # 旋转扩增
│   ├── flip/                  # 翻转扩增
│   ├── color/                 # 颜色扩增
│   ├── combined/              # 组合扩增
│   └── augmentation_stats.json # 扩增统计 (4KB)
├── output/                     # 输出数据
├── cache/                      # 缓存数据
├── notebooks/                  # Jupyter 笔记本
└── scripts/                    # 脚本文件
```

### experiments 目录 (`/experiments/`)
实验数据组织：
```
/experiments/
├── exp-001-baseline/           # 基线实验
├── exp-002-augmented/          # 扩增数据实验
├── exp-003-filtered/           # 过滤数据实验
├── exp-004-mixed/              # 混合数据实验
└── experiment_results.xlsx     # 实验结果 (50KB)
```

### models 目录 (`/models/`)
模型文件组织：
```
/models/
├── checkpoints/                # 检查点文件
├── final/                      # 最终模型
├── pretrained/                 # 预训练模型
└── model_config.json           # 模型配置 (3KB)
```

---

## 🎨 UI 改进

### 1. 存储卷选择卡片增强
- ✅ 显示存储池名称
- ✅ 使用率进度条（带颜色区分）
  - 紫色：使用率 < 70%
  - 黄色：使用率 70-90%
  - 红色：使用率 > 90%
- ✅ 完整的容量信息显示

### 2. 目录浏览增强
- ✅ 显示项目数量统计
- ✅ 空状态处理
  - 无搜索结果提示
  - 空目录提示
- ✅ 文件信息完善
  - 智能文件大小显示（KB/MB）
  - 修改时间显示
  - 双击进入提示
- ✅ 使用 ScrollArea 优化长列表显示
- ✅ 操作提示："单击选择 • 双击进入"

### 3. 配置步骤优化
- ✅ 自动推荐数据集名称（从路径提取）
- ✅ 提示用户名称可修改
- ✅ 增强的数据源信息显示
- ✅ 说明数据集引用机制（不复制数据）
- ✅ 更具体的描述占位文本

### 4. 用户体验提升
- ✅ 所有步骤都有清晰的说明
- ✅ 操作提示始终可见
- ✅ 加载状态明确
- ✅ 选中状态高亮
- ✅ 面包屑导航

---

## 📝 典型使用场景

### 场景 1：从 workspace 创建扩增数据集
```
用户操作流程：
1. 选择存储卷：work-volume (500GB, 60% 使用)
2. 浏览目录：/ → workspace/ → augmented/ → combined/
3. 选择目录：/workspace/augmented/combined/
4. 系统自动填充名称："combined"
5. 修改名称为："imagenet-2024-augmented-3x"
6. 选择类型：图像数据
7. 填写描述："基于 ImageNet-2024 进行 3 倍数据扩增"
8. 创建完成
```

### 场景 2：注册已处理的数据集
```
用户操作流程：
1. 选择存储卷：data-storage (1TB, 45% 使用)
2. 浏览目录：/ → datasets/ → cifar10-augmented/
3. 选择目录：/datasets/cifar10-augmented/
4. 使用推荐名称："cifar10-augmented"
5. 选择类型：图像数据
6. 选择可用区：cn-north-1a
7. 创建完成
```

### 场景 3：创建实验数据集
```
用户操作流程：
1. 选择存储卷：exp-storage (200GB, 30% 使用)
2. 浏览目录：/ → experiments/ → exp-002-augmented/
3. 选择目录：/experiments/exp-002-augmented/
4. 修改名称为："exp-002-dataset"
5. 选择类型：混合数据
6. 可见性：私有
7. 创建完成
```

---

## 🔧 技术实现细节

### 目录结构生成逻辑

```typescript
// 根据路径动态生成目录内容
const loadDirectories = async (path: string) => {
  // 不同路径返回不同的目录结构
  switch (path) {
    case '/':
      return rootDirectories;
    case '/datasets/':
      return datasetDirectories;
    case '/datasets/imagenet-2024/':
      return imagenetStructure;
    // ... 更多路径
  }
};
```

### 智能名称推荐

```typescript
// 从路径提取目录名作为推荐名称
const pathParts = selectedPath.split('/').filter(Boolean);
const suggestedName = pathParts[pathParts.length - 1];
```

### 使用率颜色逻辑

```typescript
const getUsageColor = (usage: number) => {
  if (usage > 90) return 'bg-red-500';
  if (usage > 70) return 'bg-yellow-500';
  return 'bg-purple-500';
};
```

---

## 🎯 数据特点

### 真实性
- 使用真实的数据集名称（ImageNet, CIFAR-10, COCO）
- 合理的文件大小和时间戳
- 符合实际项目的目录组织

### 层次性
- 3-4 层深度的目录结构
- 支持递归浏览
- 面包屑导航支持

### 多样性
- 图像数据集
- 文本语料库
- 音频和视频示例
- 实验数据
- 模型文件

### 教育性
- 清晰的命名约定
- 版本号管理示例
- 数据处理工作流体现

---

## 📈 未来扩展建议

### 短期改进
1. **实际 API 集成**
   - 连接真实的文件系统 API
   - 支持大型目录的分页加载
   - 实现真实的文件大小查询

2. **预览功能**
   - 图片缩略图预览
   - 文本文件内容预览
   - JSON/YAML 文件格式化显示

3. **批量操作**
   - 支持多选目录
   - 批量创建数据集
   - 数据集合并功能

### 长期规划
1. **智能分析**
   - 自动检测数据类型
   - 数据质量评估
   - 文件结构验证

2. **高级过滤**
   - 按文件类型过滤
   - 按大小范围过滤
   - 按修改时间过滤

3. **协作功能**
   - 目录权限管理
   - 共享数据集
   - 评论和标注

---

---

## 🐛 Bug 修复

### 存储卷状态过滤问题
**问题**：对话框中过滤条件使用 `status === 'Active'`（大写），但 storageService 中的状态值是小写的 `'available'` 和 `'bound'`，导致无法显示任何存储卷。

**修复**：
```typescript
// 修复前
setVolumes(data.filter(v => v.status === 'Active'));

// 修复后
setVolumes(data.filter(v => v.status === 'available' || v.status === 'bound'));
```

### 存储卷类型字段问题
**问题**：卡片中使用了 `volume.type`，但实际字段名是 `volume.poolType`。

**修复**：
```typescript
// 修复前
{volume.type}

// 修复后
{volume.poolType === 'file' ? '文件存储' : '对象存储'}
```

### 使用率计算问题
**问题**：直接使用 `volume.usage`（是一个对象）作为百分比。

**修复**：
```typescript
// 修复前
volume.usage > 90

// 修复后
(volume.usedGB / volume.capacityGB * 100) > 90
```

---

## 📦 可用的模拟存储卷

系统现在会显示以下 3 个存储卷（来自 storageService.ts）：

### 1. zhangsan-datasets
- **ID**: vol-001
- **类型**: 文件存储 (CephFS SSD)
- **容量**: 500GB
- **已使用**: 342GB (68.4%)
- **访问模式**: RWX（读写多节点）
- **状态**: bound（已绑定）
- **挂载点**: 
  - Jupyter 环境: /workspace/data
  - 训练任务: /data

### 2. ai-team-shared
- **ID**: vol-002
- **类型**: 文件存储 (CephFS HDD)
- **容量**: 2000GB
- **已使用**: 1256GB (62.8%)
- **访问模式**: RWX
- **状态**: bound
- **挂载点**: Jupyter 环境: /workspace/shared

### 3. model-weights-archive
- **ID**: vol-003
- **类型**: 对象存储 (MinIO)
- **容量**: 5000GB
- **已使用**: 3420GB (68.4%)
- **访问模式**: RWX
- **状态**: available（可用）
- **挂载点**: 无

---

**更新日期**：2024-11-14  
**版本**：v1.1  
**状态**：✅ 完成 + Bug 修复
