# 算力使用监控模块 - 原型开发设计方案 📊

## 一、功能概述

### 🎯 核心目标
让运营者能快速回答：
- "过去7天，A 企业用了多少 GPU 小时？主要用在哪些项目？"
- "B 部门的 V100 使用率是否异常下降？"
- "导出 C 用户组本月所有容器实例的明细账单"

### 📌 核心特性
- ✅ **双轴穿透**：时间 × 组织（企业/部门/用户组/个人）
- ✅ **多维查询**：资源类型、规格、可用区、时间范围
- ✅ **统计分析**：GPU小时、CPU核时、存储TB·天、费用
- ✅ **趋势分析**：日/周/月粒度折线图
- ✅ **排行榜**：Top N 企业/用户组/个人
- ✅ **数据导出**：CSV/Excel，支持异步大数据量

---

## 二、数据模型设计

### 2.1 核心事实表：compute_usage_records

```sql
CREATE TABLE compute_usage_records (
    -- 主键
    record_id VARCHAR(64) PRIMARY KEY,
    
    -- 用户信息（冗余，避免实时JOIN）
    user_id VARCHAR(64) NOT NULL,
    user_name VARCHAR(255) NOT NULL,
    
    -- 组织信息（冗余）
    enterprise_id VARCHAR(64),
    enterprise_name VARCHAR(255),
    department_id VARCHAR(64),
    department_name VARCHAR(255),
    user_group_id VARCHAR(64),
    user_group_name VARCHAR(255),
    
    -- 资源信息
    resource_type VARCHAR(20) NOT NULL,  -- gpu, cpu, storage
    resource_spec VARCHAR(100) NOT NULL, -- A100-80GB, 32C64G
    instance_id VARCHAR(64) NOT NULL,    -- 容器/Pod/VM ID
    zone_id VARCHAR(64),                 -- 可用区
    
    -- 时间信息
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP NOT NULL,
    duration_seconds INT NOT NULL,       -- 实际使用时长（秒）
    
    -- 计算指标（预计算，加速聚合）
    gpu_hours DECIMAL(12, 2),            -- GPU 小时数
    cpu_core_hours DECIMAL(12, 2),       -- CPU 核时
    storage_tb_days DECIMAL(12, 4),      -- 存储 TB·天
    
    -- 费用信息
    cost_amount DECIMAL(12, 2),          -- 折扣前费用
    final_amount DECIMAL(12, 2),         -- 实际计费金额
    
    -- 索引
    INDEX idx_user_id (user_id),
    INDEX idx_enterprise_id (enterprise_id),
    INDEX idx_department_id (department_id),
    INDEX idx_user_group_id (user_group_id),
    INDEX idx_resource_type (resource_type),
    INDEX idx_start_time (start_time),
    INDEX idx_end_time (end_time),
    
    -- 复合索引（优化常见查询）
    INDEX idx_enterprise_time (enterprise_id, start_time, end_time),
    INDEX idx_resource_time (resource_type, start_time, end_time)
);
```

### 2.2 组织架构表（用于下钻分析）

```sql
-- 企业表
CREATE TABLE enterprises (
    enterprise_id VARCHAR(64) PRIMARY KEY,
    enterprise_name VARCHAR(255) NOT NULL,
    status VARCHAR(20) DEFAULT 'ACTIVE'
);

-- 部门表
CREATE TABLE departments (
    department_id VARCHAR(64) PRIMARY KEY,
    department_name VARCHAR(255) NOT NULL,
    enterprise_id VARCHAR(64) NOT NULL,
    parent_department_id VARCHAR(64),
    FOREIGN KEY (enterprise_id) REFERENCES enterprises(enterprise_id)
);

-- 用户组表
CREATE TABLE user_groups (
    user_group_id VARCHAR(64) PRIMARY KEY,
    user_group_name VARCHAR(255) NOT NULL,
    enterprise_id VARCHAR(64),
    department_id VARCHAR(64),
    FOREIGN KEY (enterprise_id) REFERENCES enterprises(enterprise_id)
);
```

---

## 三、业务流程设计

### 3.1 数据采集流程

```
容器生命周期事件
    ↓
记录使用时长和资源规格
    ↓
查询定价和折扣信息
    ↓
计算 GPU小时/CPU核时/费用
    ↓
插入 compute_usage_records 表
    ↓
异步聚合到 daily_usage_summary 表
```

### 3.2 查询流程

```
用户选择时间范围和组织维度
    ↓
构建查询条件（WHERE子句）
    ↓
执行聚合查询（GROUP BY）
    ↓
返回统计指标（GPU小时、费用等）
    ↓
渲染图表和表格
```

### 3.3 导出流程

```
用户配置导出选项
    ↓
估算数据量
    ↓
< 10万行：同步导出 → 直接下载
    ↓
≥ 10万行：异步导出 → 后台任务 → 邮件通知
```

---

## 四、页面结构设计

### 4.1 页面层级

```
ComputeUsageMonitoringPage (父容器)
├── UsageOverviewTab (总览页)
│   ├── TimeRangeSelector (时间选择器)
│   ├── OrganizationFilter (组织筛选器)
│   ├── ResourceTypeSelector (资源类型选择)
│   ├── MetricCards (指标卡片)
│   ├── UsageTrendChart (趋势图)
│   └── TopEnterprisesRanking (Top N 排行)
│
├── DetailDrilldownTab (下钻分析页)
│   ├── OrganizationTree (组织树)
│   ├── ResourceDistributionChart (资源分布饼图)
│   ├── UsageDetailTable (使用明细表)
│   └── Pagination (分页器)
│
└── ExportDialog (导出配置弹窗)
    ├── ExportScopeSelector (导出范围)
    ├── FormatSelector (格式选择)
    └── FieldSelector (字段选择)
```

### 4.2 路由设计

```typescript
/compute-usage-monitoring          // 总览页（默认）
/compute-usage-monitoring/overview // 总览页
/compute-usage-monitoring/drilldown/:enterpriseId  // 下钻分析
/compute-usage-monitoring/export   // 导出历史
```

---

## 五、核心功能模块设计

### 5.1 时间维度选择器

**快捷选项**:
- 今天
- 昨天
- 最近7天
- 最近30天
- 本周
- 本月
- 自定义范围

**交互**:
- 点击快捷选项直接切换
- 自定义范围打开日期选择器

### 5.2 组织维度筛选器

**四级联动**:
```
企业（一级）
  └─ 部门（二级）
      └─ 用户组（三级）
          └─ 个人（四级）
```

**交互**:
- 级联下拉选择
- 支持搜索
- 支持全选/反选

### 5.3 聚合指标卡片

**指标类型**:
1. **GPU 小时数**
   - 数值：128,450 h
   - 环比：+12.3% ↑

2. **CPU 核时**
   - 数值：45,230 核·h
   - 环比：-5.6% ↓

3. **存储 TB·天**
   - 数值：320.5 TB·天
   - 环比：+8.9% ↑

4. **总费用**
   - 数值：¥ 328,450
   - 环比：+15.3% ↑

### 5.4 趋势图表

**图表类型**:
- 折线图（多系列）
- X轴：时间（日期）
- Y轴：GPU小时 / CPU核时 / 费用

**交互**:
- 悬停显示 Tooltip
- 点击图例切换系列显示/隐藏
- 支持缩放和平移

### 5.5 Top N 排行榜

**排行维度**:
- Top 5 企业（按 GPU 小时）
- Top 5 用户组（按费用）
- Top 10 个人用户（按使用时长）

**展示信息**:
- 排名
- 名称
- 指标值
- 占比（饼图/进度条）

### 5.6 使用明细表格

**列定义**:
| 列名 | 说明 | 排序 | 筛选 |
|------|------|------|------|
| 用户名 | user_name | ✅ | ✅ |
| 企业 | enterprise_name | ✅ | ✅ |
| 部门 | department_name | ✅ | ✅ |
| 用户组 | user_group_name | ✅ | ✅ |
| 实例ID | instance_id | ❌ | ✅ |
| 资源类型 | resource_type | ✅ | ✅ |
| 资源规格 | resource_spec | ✅ | ✅ |
| 可用区 | zone_id | ✅ | ✅ |
| 开始时间 | start_time | ✅ | ❌ |
| 结束时间 | end_time | ✅ | ❌ |
| 时长(h) | duration_seconds/3600 | ✅ | ❌ |
| 原价(¥) | cost_amount | ✅ | ❌ |
| 折后价(¥) | final_amount | ✅ | ❌ |

**交互功能**:
- 排序（点击列头）
- 搜索（全局搜索框）
- 筛选（列筛选器）
- 分页（支持跳页）
- 列显隐（列配置面板）
- 行选择（支持批量操作）

### 5.7 数据导出

**导出配置**:
```typescript
interface ExportConfig {
  scope: 'current' | 'all';       // 当前筛选结果 / 全量数据
  format: 'csv' | 'excel';        // 导出格式
  fields: string[];               // 选中的字段
  filename?: string;              // 自定义文件名
}
```

**异步导出流程**:
1. 检查数据量
2. 若 > 10万行，创建后台任务
3. 显示任务进度
4. 完成后发送邮件通知
5. 提供下载链接（7天有效期）

---

## 六、交互原型详细设计

### 6.1 总览页原型

```
┌─────────────────────────────────────────────────────────────────┐
│ 🖥️ 算力使用监控 > 总览                                          │
├─────────────────────────────────────────────────────────────────┤
│ ⏰ 时间范围: [最近7天 ▼] [2024-11-30 至 2024-12-07]             │
│ 🏢 组织筛选: 企业 [全部 ▼] → 部门 [全部 ▼] → 用户组 [全部 ▼]    │
│ 💻 资源类型: [GPU ●] [CPU ○] [存储 ○]                           │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📊 核心指标                                                  │ │
│ ├─────────────────────────────────────────────────────────────┤ │
│ │ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐  │ │
│ │ │ GPU小时   │ │ CPU核时   │ │ 存储TB·天 │ │ 总费用    │  │ │
│ │ │ 128,450 h │ │ 45,230 h  │ │ 320.5 TB  │ │ ¥328K     │  │ │
│ │ │ ↑ +12.3%  │ │ ↓ -5.6%   │ │ ↑ +8.9%   │ │ ↑ +15.3%  │  │ │
│ │ └───────────┘ └───────────┘ └───────────┘ └───────────┘  │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📈 使用趋势（过去7天）                                       │ │
│ ├─────────────────────────────────────────────────────────────┤ │
│ │         20K ┤                                    ●           │ │
│ │  GPU小时    ├─────────●───────●──────●────●───●             │ │
│ │         15K ┤       ●                                        │ │
│ │         10K ┤     ●                                          │ │
│ │          5K ┤                                                │ │
│ │          0  └─┬────┬────┬────┬────┬────┬────┬──             │ │
│ │             11/30 12/1 12/2 12/3 12/4 12/5 12/6              │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌──────────────────┐ ┌───────────────────────────────────────┐ │
│ │ 🏆 Top 5 企业    │ │ 📦 资源类型分布                       │ │
│ ├──────────────────┤ ├───────────────────────────────────────┤ │
│ │ 1. XX科技        │ │         GPU 68%  ┌─────┐               │ │
│ │    42,100 h      │ │                  │     │               │ │
│ │ ████████████ 33% │ │         CPU 22%  │ ●   │               │ │
│ │                  │ │                  │     │               │ │
│ │ 2. YY研究院      │ │         存储 10% └─────┘               │ │
│ │    31,200 h      │ │                                        │ │
│ │ █████████ 24%    │ │                                        │ │
│ │                  │ │                                        │ │
│ │ 3. ZZ大学        │ │                                        │ │
│ │    20,500 h      │ │                                        │ │
│ │ ██████ 16%       │ │                                        │ │
│ │ ...              │ │                                        │ │
│ └──────────────────┘ └───────────────────────────────────────┘ │
│                                                                 │
│ [导出当前视图] [下钻分析] [刷新]                                │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 下钻分析页原型

```
┌─────────────────────────────────────────────────────────────────┐
│ 🖥️ 算力使用监控 > XX科技（2024-11-30 至 2024-12-07）            │
├─────────────────────────────────────────────────────────────────┤
│ [← 返回总览] [🔄 刷新] [📥 导出明细]                             │
├─────────────┬───────────────────────────────────────────────────┤
│ 🌳 组织结构 │ 📊 使用详情                                       │
│             │                                                   │
│ 🏢 XX科技   │ 当前选中: 📁 CV实验室                             │
│ ├─ 算法部   │                                                   │
│ │  ├─ CV实验室│ ┌────────────────────────────────────────────┐ │
│ │  │  (选中)  │ │ 资源分布                                   │ │
│ │  └─ NLP组 │ ├────────────────────────────────────────────┤ │
│ └─ 运维部   │ │ A100-80GB: 68%  ████████████████████        │ │
│             │ │ V100-32GB: 22%  ██████████                  │ │
│             │ │ CPU集群:   10%  ████                        │ │
│             │ └────────────────────────────────────────────┘ │
│             │                                                   │
│             │ ┌──��────────────────────────────────────────────┐│
│             │ │ 📋 使用明细表格          [🔍 搜索] [⚙️ 列配置]││
│             │ ├───────────────────────────────────────────────┤│
│             │ │用户│实例ID│资源规格│开始时间│时长(h)│费用(¥) │││
│             │ ├───────────────────────────────────────────────┤│
│             │ │张三│pod-a1│A100-80│06-05  │12.5   │250.00  │││
│             │ │    │b2    │GB     │14:23  │       │        │││
│             │ │李四│pod-c3│V100-32│06-06  │8.2    │98.40   │││
│             │ │    │d4    │GB     │09:10  │       │        │││
│             │ │王五│pod-e5│A100-80│06-06  │24.0   │480.00  │││
│             │ │    │f6    │GB     │00:00  │       │        │││
│             │ │... │...   │...    │...    │...    │...     │││
│             │ └───────────────────────────────────────────────┘│
│             │                                                   │
│             │ 分页: [1] [2] ... [10]  每页 [20 ▼] 行  共 183条│
└─────────────┴───────────────────────────────────────────────────┘
```

### 6.3 导出配置弹窗

```
┌──────────────────────────────────────────┐
│ 📥 导出使用记录                          │
├──────────────────────────────────────────┤
│                                          │
│ 📦 导出范围:                             │
│ ● 当前筛选结果（推荐）                   │
│   预计 1,234 条记录                      │
│ ○ 全量数据                               │
│   预计 1,234,567 条记录（异步导出）      │
│                                          │
│ 📄 导出格式:                             │
│ ● CSV（兼容Excel）                       │
│ ○ Excel (.xlsx)                          │
│                                          │
│ ✅ 字段选择:                             │
│ ☑ 用户名    ☑ 企业       ☑ 部门         │
│ ☑ 用户组    ☑ 资源类型   ☑ 资源规格     │
│ ☑ 实例ID    ☑ 可用区     ☑ 开始时间     │
│ ☑ 结束时间  ☑ 使用时长   ☑ GPU小时      │
│ ☑ 原价      ☑ 折后价                    │
│                                          │
│ 📝 文件名（可选）:                       │
│ [算力使用明细_2024-12-07              ] │
│                                          │
│ ⚠️ 提示: 数据量较大时将转为异步导出     │
│                                          │
│        [取消]        [确认导出]          │
└──────────────────────────────────────────┘
```

---

## 七、技术实现方案

### 7.1 前端技术栈

- **框架**: React 18 + TypeScript
- **UI组件**: Shadcn/ui + Tailwind CSS
- **图表库**: Recharts
- **状态管理**: React Hooks (useState, useEffect)
- **路由**: React Router
- **HTTP客户端**: Fetch API
- **日期处理**: date-fns

### 7.2 核心组件设计

#### 时间范围选择器
```typescript
interface TimeRange {
  start: Date;
  end: Date;
  label: string;  // "最近7天"
}

const TimeRangeSelector: React.FC<{
  value: TimeRange;
  onChange: (range: TimeRange) => void;
}> = ({ value, onChange }) => {
  // 实现逻辑
};
```

#### 组织树选择器
```typescript
interface OrganizationNode {
  id: string;
  name: string;
  type: 'enterprise' | 'department' | 'user_group' | 'user';
  children?: OrganizationNode[];
}

const OrganizationTree: React.FC<{
  data: OrganizationNode[];
  onSelect: (node: OrganizationNode) => void;
}> = ({ data, onSelect }) => {
  // 实现逻辑
};
```

### 7.3 API接口设计

```typescript
// 查询聚合统计
GET /api/v1/compute-usage/aggregate
Query:
  - start_time: string (ISO 8601)
  - end_time: string
  - enterprise_id?: string
  - department_id?: string
  - user_group_id?: string
  - resource_type?: string
Response:
{
  "totalGpuHours": 128450.5,
  "totalCpuCoreHours": 45230.2,
  "totalStorageTbDays": 320.5,
  "totalCost": 328450.00,
  "trend": [
    { "date": "2024-11-30", "gpuHours": 18200 },
    // ...
  ]
}

// 查询使用明细
GET /api/v1/compute-usage/records
Query:
  - start_time, end_time
  - enterprise_id?, department_id?, user_group_id?, user_id?
  - resource_type?, resource_spec?
  - page: number
  - page_size: number
Response:
{
  "total": 1234,
  "records": [
    {
      "recordId": "rec-001",
      "userId": "user-001",
      "userName": "张三",
      // ...
    }
  ]
}

// 导出数据
POST /api/v1/compute-usage/export
Body:
{
  "scope": "current",
  "format": "csv",
  "fields": ["userName", "enterpriseName", ...],
  "filters": { ... }
}
Response:
{
  "taskId": "task-001",     // 异步任务ID
  "downloadUrl": "...",     // 同步导出时返回
  "estimatedTime": 120      // 预计时间（秒）
}
```

### 7.4 性能优化策略

#### 1. 数据预聚合
```sql
-- 创建每日汇总表
CREATE TABLE daily_usage_summary (
    summary_date DATE,
    enterprise_id VARCHAR(64),
    department_id VARCHAR(64),
    user_group_id VARCHAR(64),
    resource_type VARCHAR(20),
    total_gpu_hours DECIMAL(12, 2),
    total_cpu_core_hours DECIMAL(12, 2),
    total_storage_tb_days DECIMAL(12, 4),
    total_cost DECIMAL(12, 2),
    PRIMARY KEY (summary_date, enterprise_id, resource_type)
);

-- 每日凌晨自动聚合
INSERT INTO daily_usage_summary
SELECT 
    DATE(start_time) AS summary_date,
    enterprise_id,
    department_id,
    user_group_id,
    resource_type,
    SUM(gpu_hours),
    SUM(cpu_core_hours),
    SUM(storage_tb_days),
    SUM(final_amount)
FROM compute_usage_records
WHERE DATE(start_time) = CURRENT_DATE - INTERVAL 1 DAY
GROUP BY DATE(start_time), enterprise_id, department_id, user_group_id, resource_type;
```

#### 2. 前端缓存策略
- 使用 React Query 缓存 API 响应（5分钟）
- 图表数据本地缓存（避免重复渲染）
- 组织树数据全局缓存（很少变化）

#### 3. 分页和虚拟滚动
- 明细表格使用服务端分页
- 长列表使用虚拟滚动（react-window）
- 默认每页20条，最大100条

#### 4. 异步导出
```typescript
// 数据量阈值
const SYNC_EXPORT_THRESHOLD = 100000;

if (recordCount < SYNC_EXPORT_THRESHOLD) {
  // 同步导出：直接生成文件并下载
  return generateCsvAndDownload(records);
} else {
  // 异步导出：创建后台任务
  const taskId = await createExportTask(config);
  return { taskId, status: 'processing' };
}
```

---

## 八、开发任务拆解

### Phase 1: 数据层和服务层（第1周）
- [ ] 设计并创建数据库表
- [ ] 创建 TypeScript 类型定义
- [ ] 实现 `computeUsageService.ts`
- [ ] 实现模拟数据生成器
- [ ] 单元测试

### Phase 2: 总览页（第2周）
- [ ] 时间范围选择器组件
- [ ] 组织筛选器组件
- [ ] 指标卡片组件
- [ ] 趋势图表组件（Recharts）
- [ ] Top N 排行组件
- [ ] 页面集成和联调

### Phase 3: 下钻分析页（第3周）
- [ ] 组织树组件
- [ ] 资源分布饼图
- [ ] 使用明细表格（带排序、筛选、分页）
- [ ] 列配置面板
- [ ] 页面集成

### Phase 4: 数据导出（第4周）
- [ ] 导出配置弹窗
- [ ] CSV 生成器
- [ ] Excel 生成器（可选）
- [ ] 异步导出任务管理
- [ ] 导出历史页面

### Phase 5: 优化和测试（第5周）
- [ ] 性能优化（缓存、分页）
- [ ] 权限控制（RBAC）
- [ ] 响应式适配
- [ ] 完整功能测试
- [ ] 文档编写

---

## 九、质量保证

### 9.1 性能指标
- 总览页加载时间 < 2秒
- 下钻分析页加载时间 < 3秒
- 明细表格切换页面 < 500ms
- 图表渲染 < 1秒

### 9.2 兼容性
- 浏览器：Chrome 90+, Firefox 88+, Safari 14+
- 屏幕分辨率：1366×768 及以上
- 移动端：响应式适配（平板可用）

### 9.3 权限隔离
- 运营A只能看其负责的企业
- 企业管理员只能看本企业数据
- 基于 RBAC 实现细粒度权限控制

---

## 十、总结

### ✅ 核心价值
1. **效率提升**：从 SQL 查询（30分钟）→ 可视化点击（30秒）
2. **决策支持**：实时了解算力使用情况，优化资源分配
3. **成本透明**：清晰展示各组织的费用，支持成本核算
4. **审计支持**：完整的使用记录，支持合规审计

### 🚀 扩展方向
1. 告警功能：使用量/费用异常告警
2. 预测功能：基于历史数据预测未来用量
3. 对比功能：同比/环比分析
4. 自定义报表：用户自定义图表和指标

---

**文档版本**: v1.0.0  
**编写日期**: 2024-12-07  
**维护团队**: 费米集群开发团队
